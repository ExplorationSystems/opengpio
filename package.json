{
  "dependencies": {
    "bindings": "^1.5.0"
  },
  "name": "opengpio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-gyp configure build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "node-addon-api": "^7.0.0"
  }
}
